apiVersion: 1

providers:
  - name: "llm-gateway"
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      # Dashboards get added here in the observability phase.
      path: /etc/grafana/provisioning/dashboards
